/**
 * @module noop
 * @description A no-operation function used as a default/placeholder callback
 * throughout the player. Commonly assigned to timer callbacks, event listeners,
 * and metric recorders when the actual implementation is not yet initialized
 * or has been disposed.
 *
 * @original Module_10911
 */

/**
 * No-operation function. Does nothing when called.
 * @returns {void}
 */
export const noop = function () {};

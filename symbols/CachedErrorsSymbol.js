/**
 * Netflix Cadmium Player — Cached Errors Symbol
 *
 * Dependency-injection token for the cached errors service, which stores
 * and retrieves previously encountered errors to avoid redundant reporting.
 *
 * @module CachedErrorsSymbol
 */

/**
 * DI token string for the cached errors service.
 * @type {string}
 */
export const CachedErrorsSymbol = "CachedErrorsSymbol";

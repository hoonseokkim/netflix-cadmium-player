/**
 * Manifest Factory Symbol
 *
 * Dependency injection symbol for the manifest factory service.
 * The manifest factory is responsible for creating and parsing
 * streaming manifest objects (DASH MPD / Netflix custom manifests).
 *
 * @module symbols/ManifestFactorySymbol
 * @original Module_17398
 */

/**
 * Symbol identifier for the ManifestFactory service in the DI container.
 * @type {string}
 */
export const ManifestFactorySymbol = "ManifestFactorySymbol";
